name: smart_mpasi
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2


dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  image_picker: ^1.2.0
  provider: ^6.0.5
  intl: ^0.20.2
  fl_chart: ^1.1.1
  google_fonts: ^6.2.0



dev_dependencies:
  flutter_test:

    sdk: flutter


  flutter_lints: ^6.0.0






flutter:


  uses-material-design: true

  # Daftar aset yang digunakan dalam aplikasi
  assets:
    - assets/images/logo_baru.png

    # Atau jika ingin load semua file di folder images, gunakan: